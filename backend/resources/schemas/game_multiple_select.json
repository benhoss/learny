{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MultipleSelectGame",
  "type": "object",
  "required": ["questions"],
  "properties": {
    "title": { "type": "string" },
    "intro": { "type": "string" },
    "estimated_time_minutes": { "type": "integer", "minimum": 1 },
    "difficulty": { "type": "number", "minimum": 0, "maximum": 1 },
    "questions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["prompt", "choices", "answer_indices"],
        "properties": {
          "prompt": { "type": "string" },
          "choices": {
            "type": "array",
            "minItems": 2,
            "items": { "type": "string" }
          },
          "answer_indices": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "integer", "minimum": 0 }
          },
          "hint": { "type": "string" },
          "explanation": { "type": "string" },
          "topic": { "type": "string" },
          "difficulty": { "type": "number", "minimum": 0, "maximum": 1 }
        }
      }
    }
  }
}
